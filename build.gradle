
buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath ANDROID_GRADLE
        classpath RETROLAMBDA
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
